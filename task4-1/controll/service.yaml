apiVersion: v1
kind: Service
metadata:
  name: task4-1-service
spec:
  selector:
    app: task4-1 # Seleciona os pods do StatefulSet usando o rótulo 'app'
  ports:
    - protocol: TCP
      port: 80 # Porta do serviço
      targetPort: 7000 # Porta do contêiner
  type: ClusterIP
  